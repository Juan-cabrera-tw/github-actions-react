name: Hello World
author: Juan Cabrera
description: jejejeje
inputs:
  who-to-greet:
    description: "Who to greet"
    required: true
    default: Juan
outputs:
  time:
    description: "the greeting time"
runs:
  using: "docker"
  image: "Dockerfile"
  # entrypoint:
  args:
    - ${{ inputs.who-to-greet }} 
